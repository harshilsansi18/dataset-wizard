
import { useToast, toast } from "@/hooks/use-toast";
import type { ToastProps, ToastActionElement, ToastType } from "@/hooks/use-toast";

export { useToast, toast };
export type { ToastProps, ToastActionElement, ToastType };
